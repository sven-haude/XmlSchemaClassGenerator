# AGENT Instructions: xscgen Tool

`xscgen` is the .NET CLI tool wrapper for the console application.

## Build
Use `dotnet build` or `dotnet pack` in this folder to produce the tool package.

## Notes
- References the main library and console project.
- Follows the repository code style (4 spaces, file-scoped namespaces).
