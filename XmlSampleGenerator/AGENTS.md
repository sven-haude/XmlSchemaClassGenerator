# AGENT Instructions: XmlSampleGenerator

This project provides helper classes used during testing to generate XML samples.

## Build
Run `dotnet build` here or via the solution file.

## Style
- Older C# code but keep 4‑space indentation.
- Maintain compatibility with `netstandard2.0`.
