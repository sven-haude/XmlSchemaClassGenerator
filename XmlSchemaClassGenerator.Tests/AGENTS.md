# AGENT Instructions: Unit Tests

This project contains the xUnit test suite for the code generator.

## Running Tests
Execute `dotnet test` from the repository root or from this directory to run all tests.

## Code Style
- Use the same formatting as the production code (4 spaces, file-scoped namespaces, braces on new lines).
- New tests should be added under this project.
