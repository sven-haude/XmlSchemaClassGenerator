# AGENT Instructions: xscgen-proj Tool

`xscgen-proj` is an alternative CLI package which also wraps the console application.

## Build
Run `dotnet build` or `dotnet pack` here to create the package.

## Notes
- Project references mirror the `xscgen` tool.
- Adhere to the repository coding conventions (4 spaces, file-scoped namespaces).
